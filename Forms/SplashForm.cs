using System;
using System.Windows.Forms;

namespace RestaurantOrderingSystem.Forms
{
    // SplashForm is the first window the user sees while the application starts.
    // Phase 4 will give it a logo, version label, and an auto-close timer.
    // For now it is intentionally empty so the project compiles and runs.
    public partial class SplashForm : Form
    {
        public SplashForm()
        {
            InitializeComponent();
        }
    }
}
